.modal-footer
  -if current_user
    =form_for([@picture, @picture.comments.build], remote:true) do |f|
      =f.label 'Write comment', class: 'comment-label'
      =f.text_field :text, class: 'comment-textfield'
      =f.submit 'Send comment', id: 'comment-submit'